define(["step", "whispeerHelper", "asset/observer"], function () {
	"use strict";
	var service = function () {
		function syncedList(endPoint, cacheName, id, ordered) {

		}

		syncedList.prototype.load = function () {};
		syncedList.prototype.update = function () {};
	};

	service.$inject = [];

	return service;
});